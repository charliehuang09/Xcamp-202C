#include <iostream>
#include <vector>
#include <algorithm>
#include <set>
#include <stdio.h>
#include <stack>
using namespace std;
int getint(){
    return 0;
}
string recurssion(string input){
    string t;
    int t1;
    for (int i = 0; i < input.size(); i++){
        if (input[i] == '('){
            i++;
            t = "";
            while (i < input.size() && input[i] != ')'){
                t += input[i];
                i++;
            }
            t = recurssion(t);

        }
    }
    for (int i = 0; i < input.size(); i++){
        if (input[i] == '*'){

        }
    }
}
int main() {
    string input;
    cin >> input;
    cout << recurssion(input);

}
//2^3+5/3*(231-130)+10000